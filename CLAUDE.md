# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`techmg` — a technology management platform for companies. Java-based (per `.gitignore`).

## Current State

This is a fresh repository with no source code, build system, or tests yet. Only a `README.md`, `.gitignore` (Java defaults), and an initial commit exist.
